import { GetWeeksCountPipe } from './get-weeks-count.pipe';

describe('GetWeeksCountPipe', () => {
  it('create an instance', () => {
    const pipe = new GetWeeksCountPipe();
    expect(pipe).toBeTruthy();
  });
});
