import { GetFullDayEventsPipe } from './get-full-day-events.pipe';

describe('GetFullDayEventsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFullDayEventsPipe();
    expect(pipe).toBeTruthy();
  });
});
