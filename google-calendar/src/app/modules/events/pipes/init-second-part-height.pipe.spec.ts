import { InitSecondPartHeightPipe } from './init-second-part-height.pipe';

describe('InitSecondPartHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new InitSecondPartHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
