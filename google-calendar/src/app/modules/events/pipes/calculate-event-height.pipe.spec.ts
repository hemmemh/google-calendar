import { CalculateEventHeightPipe } from './calculate-event-height.pipe';

describe('CalculateEventHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateEventHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
