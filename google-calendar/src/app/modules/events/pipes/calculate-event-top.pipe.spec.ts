import { CalculateEventTopPipe } from './calculate-event-top.pipe';

describe('CalculateEventTopPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateEventTopPipe();
    expect(pipe).toBeTruthy();
  });
});
