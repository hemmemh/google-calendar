import { GetEventInfoDatePipe } from './get-event-info-date.pipe';

describe('GetEventInfoDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetEventInfoDatePipe();
    expect(pipe).toBeTruthy();
  });
});
