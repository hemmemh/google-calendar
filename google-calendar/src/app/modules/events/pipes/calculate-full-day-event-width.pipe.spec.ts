import { CalculateFullDayEventWidthPipe } from './calculate-full-day-event-width.pipe';

describe('CalculateFullDayEventWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateFullDayEventWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
