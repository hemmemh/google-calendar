import { CalculateFullDayEventLeftPipe } from './calculate-full-day-event-left.pipe';

describe('CalculateFullDayEventLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateFullDayEventLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
