import { DraggableFullDayDirective } from './draggable-full-day.directive';

describe('DraggableFullDayDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableFullDayDirective();
    expect(directive).toBeTruthy();
  });
});
