import { CreateFullDayEventDirective } from './create-full-day-event.directive';

describe('CreateFullDayEventDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateFullDayEventDirective();
    expect(directive).toBeTruthy();
  });
});
