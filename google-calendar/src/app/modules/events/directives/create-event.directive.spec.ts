import { CreateEventDirective } from './create-event.directive';

describe('CreateEventDirective', () => {
  it('should create an instance', () => {
    const directive = new CreateEventDirective();
    expect(directive).toBeTruthy();
  });
});
