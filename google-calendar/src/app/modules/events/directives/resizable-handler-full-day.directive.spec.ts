import { ResizableHandlerFullDayDirective } from './resizable-handler-full-day.directive';

describe('ResizableHandlerFullDayDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizableHandlerFullDayDirective();
    expect(directive).toBeTruthy();
  });
});
